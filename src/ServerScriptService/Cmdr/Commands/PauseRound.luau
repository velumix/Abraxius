return {
	Name = "PauseRound",
	Aliases = { "Pause" },
	Description = "Toggle pausing the match loop in its current state (freezes the phase timer so you can stay in the lobby to test gifting, etc). Run again to resume.",
	Group = "DefaultAdmin",
	Args = {},
}
